[project]
name = "project-devops-deploy-infra"
version = "0.1.0"
description = "Infrastructure tooling for project-devops-deploy"
requires-python = ">=3.12"
dependencies = [
    "ansible-core==2.21.2",
]
